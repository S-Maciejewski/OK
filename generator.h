#include<cstdlib>
#include<iostream>
#include<random>

using namespace std;

class Punkt {
private:
	int x, y;
public:
	void setX(int X) { x = X; }
	void setY(int Y) { y = Y; }
	int getX() { return x; }
	int getY() { return y; }
};

Punkt* generateTable(long int size, long int range) {
		
}
